version: "3.3"

services:
  app:
    build: .
    image: sentiment-analyzer:latest
    container_name: sentiment_analyzer
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT}:80"
    volumes:
      - ./app:/app
    networks:
      - trading-framework

networks:
  trading-framework: