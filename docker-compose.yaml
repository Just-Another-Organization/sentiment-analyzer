version: "3.3"

services:
  app:
    build: .
    image: ja_sa:latest
    container_name: ja-sa
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT}:80"
    volumes:
      - ./app:/app
    networks:
      - trading-framework

networks:
  trading-framework: